$(document).on('ready page:load', function () {	
	var margin = {top: 20, right: 40, bottom: 40, left: 00};
	var view = "water_usage";

	if ($(".usage-section").length > 0) { draw(900, 360, margin, 25, 1, view); }		
}); 